#name: "docker-fastapi-demo"
version: "3.8"
services:
  backend:
    build: ./backend
    container_name: demo-backend
    expose:
      - "8000"

  frontend:
    build: ./frontend
    container_name: demo-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
